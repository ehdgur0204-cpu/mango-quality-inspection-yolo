# 이 파일은 예시(example) 설정 파일입니다.
# 실제로 사용할 때는 이 파일을 복사해 configs/data.local.yaml 을 만들고,
# path 값을 본인 PC의 절대경로로 수정하세요.
# configs/data.local.yaml 은 .gitignore에 등록되어 있어 Git에 올라가지 않습니다.

path: /absolute/path/to/project/data/processed

train: images/train
val: images/val
test: images/test

names:
  0: Alternaria
  1: Anthracnose
  2: Healthy
  3: Scab
